---
- name: Open ports
  include_role:
    name: ufw_rules
  vars:
    ufw_rules:
      - { rule: allow, port: 53, proto: tcp, comment: DNS bind }
      - { rule: allow, port: 53, proto: udp, comment: DNS bind }
