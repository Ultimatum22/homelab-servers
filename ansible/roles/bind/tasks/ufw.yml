---
- name: Open ports
  include_role:
    name: ufw
  vars:
    ufw_rules:
      - { rule: allow, port: 5354, proto: tcp, comment: DNS bind }
      - { rule: allow, port: 5354, proto: udp, comment: DNS bind }
