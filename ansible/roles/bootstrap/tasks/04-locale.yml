---
- name: Set locale
  locale_gen:
    name: "{{ locale }}"
    state: present

- name: Configure default locale
  lineinfile:
    path: /etc/locale.conf
    line: 'LANG="{{ locale }}"'
    create: yes
