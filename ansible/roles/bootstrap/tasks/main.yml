---
- name: Upgrade system for {{ ansible_facts['os_family'] }}
  ansible.builtin.include_tasks: 00-packages-{{ ansible_facts['os_family'] | lower }}.yml

- name: Install generic UFW rules
  ansible.builtin.include_tasks: 20-ufw.yml
