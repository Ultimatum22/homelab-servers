http:
  middlewares:
    chain-authelia-auth:
      chain:
        middlewares:
          - middlewares-rate-limit
          - middlewares-secure-headers
          {# - middlewares-compress #}
          - middlewares-authelia